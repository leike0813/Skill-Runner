## 1. OpenSpec

- [x] 1.1 创建 `runner-common-prompt-fallback` change 工件
- [x] 1.2 补齐 proposal / design / delta spec

## 2. Runtime prompt fallback

- [x] 2.1 为 `entrypoint.prompts` 增加 `common` fallback 解析
- [x] 2.2 保持 engine-specific prompt 优先级不变

## 3. Validation

- [x] 3.1 增加 `common` fallback 单元测试
- [x] 3.2 运行目标 pytest
- [x] 3.3 运行目标 mypy
