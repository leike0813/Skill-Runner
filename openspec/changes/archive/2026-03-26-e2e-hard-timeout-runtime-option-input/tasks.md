## 1. OpenSpec Artifacts

- [x] 1.1 Add proposal, design, and delta specs for E2E hard timeout runtime option input

## 2. Management API

- [x] 2.1 Extend management skill detail with `runtime.default_options` and add runtime-options service default endpoint
- [x] 2.2 Add management route coverage for skill runtime defaults and service hard-timeout defaults

## 3. E2E Client

- [x] 3.1 Add `hard_timeout_seconds` default resolution, validation, and payload submission in E2E routes/backend
- [x] 3.2 Add run form UI control and localization for the hard-timeout spinbox
- [x] 3.3 Add E2E client integration coverage for prefill, validation, and submitted payload shape
