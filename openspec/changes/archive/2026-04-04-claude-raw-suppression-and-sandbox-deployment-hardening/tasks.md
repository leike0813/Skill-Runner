# Tasks

- [x] Tighten Claude parser raw suppression for semantic-consumed rows and enable delayed live raw flush.
- [x] Add warning-level Claude sandbox diagnostics for dependency missing, runtime failure, and blocked commands.
- [x] Expose Claude sandbox dependency warnings in CLI diagnostics and management UI status.
- [x] Install `bubblewrap` and `socat` in the runtime image and document the requirement.
- [x] Add targeted parser / live emission / CLI / UI regression tests.
