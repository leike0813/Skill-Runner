## 1. OpenSpec

- [x] 1.1 创建 `ui-shell-security-governance-unification` change 工件
- [x] 1.2 补齐 proposal / design / delta specs

## 2. Adapter profile contract

- [x] 2.1 扩展 adapter profile schema 与 loader，新增 `ui_shell` 元数据块
- [x] 2.2 为各 engine adapter profile 补齐 `ui_shell` 元数据

## 3. Session config governance

- [x] 3.1 为 `gemini/iflow/opencode/claude` 新增 `ui_shell` config assets
- [x] 3.2 将 capability provider 改成 metadata-driven，并移除手写 payload

## 4. Validation

- [x] 4.1 更新 profile loader / ui shell 回归测试
- [x] 4.2 运行目标 pytest
- [x] 4.3 运行目标 mypy
