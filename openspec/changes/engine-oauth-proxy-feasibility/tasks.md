## 1. 证据采集与边界确认

- [x] 1.1 固化四个引擎的鉴权入口证据（CLI 子命令、交互/非交互支持、回调形态）。
- [x] 1.2 固化四个引擎的凭据写入位置与隔离环境映射关系（含容器场景）。
- [x] 1.3 固化无头/远程环境下的限制项（浏览器依赖、回调端口、人工输入要求）。

## 2. 可行性建模与方案对比

- [x] 2.1 形成统一能力矩阵（可脚本化登录、challenge 可提取性、callback 可外置性、稳定性风险）。
- [x] 2.2 对比三类候选方案：CLI 委托式编排、原生 OAuth Broker、按引擎分层混合方案。
- [x] 2.3 为每个引擎输出推荐路线（Go / Conditional / No-Go）及理由。

## 3. 决策收敛与后续实施输入

- [x] 3.1 输出预研结论文档（推荐分期、成功标准、退出条件、风险缓解策略）。
- [x] 3.2 给出下一步实现型 change 的建议范围与前置条件（仅建议，不在本 change 实施）。
- [x] 3.3 执行 `openspec validate engine-oauth-proxy-feasibility --type change` 并记录结果。
