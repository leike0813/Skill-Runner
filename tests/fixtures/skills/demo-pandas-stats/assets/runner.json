{
    "id": "demo-pandas-stats",
    "version": "1.0.0",
    "name": "Demo: Pandas Data Stats",
    "description": "Calculate statistics from CSV using Pandas",
    "execution_modes": [
        "auto"
    ],
    "runtime": {
        "language": "python",
        "version": "3.11",
        "dependencies": [
            "pandas"
        ]
    },
    "schemas": {
        "input": "assets/input.schema.json",
        "parameter": "assets/parameter.schema.json",
        "output": "assets/output.schema.json"
    },
    "entrypoint": {
        "prompts": {
            "gemini": "Please analyze the CSV file `{{ input.csv_file }}`. Calculate the mean of column `{{ parameter.column }}`. Follow `SKILL.md` for the exact script path and output the FINAL result as a valid JSON object: `{\"mean\": <value>}`."
        }
    }
}
