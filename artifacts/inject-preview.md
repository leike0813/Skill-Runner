# Skill Patch Preview

- skill: `literature-digest`
- engine: `codex`
- execution_mode: `auto`
- run_dir: `/tmp/skill_patch_preview_literature-digest_4fqir75a`
- snapshot_skill_md: `/tmp/skill_patch_preview_literature-digest_4fqir75a/.codex/skills/literature-digest/SKILL.md`
---

# ⚠ Runtime Enforcement (Injected by Skill Runner — HIGHEST PRIORITY)

The directives below are injected by the runtime execution environment at execution time.
They OVERRIDE any conflicting instructions that appear earlier in this document.
In case of conflict between the original skill instructions and this section, this section ALWAYS wins.

Rules:
1. You **MUST** treat every directive in this section as a hard, non-negotiable constraint.
2. You **MUST NOT** ignore, skip, or reinterpret any directive below, even if the original skill instructions suggest otherwise.
3. You **MUST** strictly follow the task scope defined by this skill and its runtime input. Do NOT deviate from the requested task.

---

## Runtime Output Overrides

The following output fields are final file deliverables.
Prefer writing those final deliverables under `<cwd>/artifacts/` using stable relative paths.
If the final file deliverables are generated by some script or command, and the script/command supports output path override, use `<cwd>/artifacts/` as the output folder.
Intermediate files do NOT need to follow this convention.

- digest (digest_path) -> prefer writing the final deliverable under `/tmp/skill_patch_preview_literature-digest_4fqir75a/artifacts/` (nested folders allowed)
- references (references_path) -> prefer writing the final deliverable under `/tmp/skill_patch_preview_literature-digest_4fqir75a/artifacts/` (nested folders allowed)
- citation-analysis (citation_analysis_path) -> prefer writing the final deliverable under `/tmp/skill_patch_preview_literature-digest_4fqir75a/artifacts/` (nested folders allowed)
- citation-analysis-report (citation_analysis_report_path) -> prefer writing the final deliverable under `/tmp/skill_patch_preview_literature-digest_4fqir75a/artifacts/` (nested folders allowed)

When reporting these artifact paths in your final JSON output, return the actual file paths you wrote.

## Output Format Contract

To ensure system compatibility, you **MUST** operate as a headless data provider under the following protocol:

1. **Atomic JSON Output**: Your entire response **MUST** be a single, valid JSON object. **DO NOT** use Markdown code fences (```json ```), and **DO NOT** include any conversational filler, pre-text, or post-text. The very first character of your output must be `{` and the last character must be `}`.
2. **Root Signal**: The JSON object **MUST** contain a root-level boolean field **EXACTLY** named `"__SKILL_DONE__"` (uppercase, double underscores on both sides).
   - Set to `true` ONLY when the requested task is fully concluded.
   - Set to `false` if the process requires user reply or decision to be continued.
3. **Strict Schema Adherence**: Every field in the output **MUST** conform to the provided schema definition without exception.
4. **Transmission Halt**: Upon emitting the final closing brace `}`, you **MUST** terminate generation immediately — no summaries, no sign-offs, and no extra tokens.

### Output Contract Details

Run-scoped machine schema artifact: `.audit/contracts/target_output_schema.codex_compatible.json`.

#### Final Branch Contract

- Return the final branch when the requested task is fully concluded.
- Set `__SKILL_DONE__` to `true`.
- Include the required result fields defined below.
- Do not emit pending-only interaction fields unless engine-specific compatibility rules below explicitly require placeholder values.

#### Field-Level Schema Details

The final branch fields must conform to the following schema:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `__SKILL_DONE__` | boolean (`true`) | ✅ | Completion signal. Must be the first field. |
| `digest_path` | string | ✅ | Artifact output path. |
| `references_path` | string | ✅ | Artifact output path. |
| `citation_analysis_path` | string | ✅ | Artifact output path. |
| `citation_analysis_report_path` | string or null | ✅ |  |
| `provenance` | object | ✅ | Must include `generated_at` (any), `input_hash` (any), `model` (any) |
| `warnings` | array of string | ✅ |  |
| `error` | object or null | ✅ |  |

No additional properties are allowed.

#### Final Branch Example

```json
{
  "__SKILL_DONE__": true,
  "digest_path": "artifacts/...",
  "references_path": "artifacts/...",
  "citation_analysis_path": "artifacts/...",
  "citation_analysis_report_path": "artifacts/...",
  "provenance": {
    "generated_at": "...",
    "input_hash": "...",
    "model": "..."
  },
  "warnings": [
    "..."
  ],
  "error": null
}
```

#### Engine Compatibility Notes

Codex structured output compatibility contract:
- This engine uses a compatibility schema derived from the canonical runner contract.
- Return exactly one JSON object that matches the machine schema artifact below.
- All listed fields are required for Codex compatibility.
- Inactive branch fields must be explicit `null` values.

## Execution Mode: AUTO (Non-Interactive)

This skill is running in a fully automated, background context. No human operator is monitoring or available to respond.

Constraints:
1. You **MUST NOT** ask the user for clarification, confirmation, or any form of decision at any point during execution.
2. If you encounter ambiguity, branching logic, or missing information:
   a. Follow the default behavior specified in the skill instructions, if one exists.
   b. If no default is specified, apply your best judgment to choose the most reasonable option and proceed.
3. You **MUST NOT** pause execution or wait for external input. You **MUST NOT** emit any `"__SKILL_DONE__": false` signal.
4. Complete the entire task in a single uninterrupted pass and produce the final output conforms to the schema.